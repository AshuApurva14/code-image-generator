name: app

services:
  app:
   build:
     context: .
     dockerfile: Dockerfile
   ports:
     - 8000:8000

   environment:
     - SECRET_KEY={SECRET_KEY}

   networks:
     - backend_network


networks:
  backend_network:
